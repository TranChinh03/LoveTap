<UserControl x:Class="LoveTap.UserControlCustom.CustomerDetailUC"
        xmlns:uc="clr-namespace:LoveTap.UserControlCustom"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LoveTap.UserControlCustom"
        mc:Ignorable="d"
        Name="ucCustomerDetail"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        DataContext="{StaticResource DetailCustomerVM}">


    <Grid Background="Transparent">
        <Grid.RowDefinitions>
            <RowDefinition Height="2.5*"/>
            <RowDefinition Height="7.5*"/>
        </Grid.RowDefinitions>

        <!--3 Button-->
        <DockPanel Margin="15,0,15,0">
            <TextBlock FontSize="20"
                       FontWeight="Black"
                       Foreground="{StaticResource Catalina Blue}"
                       VerticalAlignment="Center"
                       >#KH001</TextBlock>

            <!--Back Button-->
            <Button DockPanel.Dock="Right" Style="{StaticResource OrangeButton}"
                    HorizontalAlignment="Right" Uid="3" Name="backBt" Content="Back">
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="Click">
                        <i:InvokeCommandAction Command="{Binding SwitchTab}"
                                                           CommandParameter="{Binding ElementName=ucCustomerDetail}"></i:InvokeCommandAction>
                    </i:EventTrigger>
                    <i:EventTrigger EventName="MouseMove">
                        <i:InvokeCommandAction Command="{Binding GetIdButton}"
                                                           CommandParameter="{Binding ElementName=backBt}"></i:InvokeCommandAction>
                    </i:EventTrigger>
                </i:Interaction.Triggers>
            </Button>

            <!--Invoice Button-->
            <Button Name="invoiceBt" Uid="2" DockPanel.Dock="Right" Style="{StaticResource greenButton}"
                    HorizontalAlignment="Right" >
                <StackPanel Orientation="Horizontal">
                    <materialDesign:PackIcon Kind="Download" />
                    <TextBlock Margin="8,0,0,0">Invoice</TextBlock>
                </StackPanel>
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="Click">
                        <i:InvokeCommandAction Command="{Binding SwitchTab}"
                                                           CommandParameter="{Binding ElementName=ucCustomerDetail}"></i:InvokeCommandAction>
                    </i:EventTrigger>
                    <i:EventTrigger EventName="MouseMove">
                        <i:InvokeCommandAction Command="{Binding GetIdButton}"
                                                           CommandParameter="{Binding ElementName=invoiceBt}"></i:InvokeCommandAction>
                    </i:EventTrigger>
                </i:Interaction.Triggers>
            </Button>

            <!--Edit Button-->
            <Button x:Name="editBt" Uid="1" DockPanel.Dock="Right" Style="{StaticResource greenButton}"
                    HorizontalAlignment="Right" Width="90" >
                <StackPanel Orientation="Horizontal">
                    <materialDesign:PackIcon Kind="Pencil" />
                    <TextBlock Margin="7,0,0,0">Edit</TextBlock>
                </StackPanel>
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="Click">
                        <i:InvokeCommandAction Command="{Binding SwitchTab}"
                                                           CommandParameter="{Binding ElementName=ucCustomerDetail}"></i:InvokeCommandAction>
                    </i:EventTrigger>
                    <i:EventTrigger EventName="MouseMove">
                        <i:InvokeCommandAction Command="{Binding GetIdButton}"
                                                           CommandParameter="{Binding ElementName=editBt}"></i:InvokeCommandAction>
                    </i:EventTrigger>
                </i:Interaction.Triggers>
            </Button>
        </DockPanel>

        <!--Detail Info-->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="3*"/>
            </Grid.ColumnDefinitions>


            <StackPanel Margin="20,0,0,0" Grid.Column="0" >
                <TextBlock Style="{StaticResource detailText}" Padding="3">Phone:</TextBlock>
                <TextBlock Style="{StaticResource detailText}" Padding="3">Name:</TextBlock>
                <TextBlock Style="{StaticResource detailText}" Padding="3">Date of Birth:</TextBlock>
                <TextBlock Style="{StaticResource detailText}" Padding="3">Address:</TextBlock>
                <TextBlock Style="{StaticResource detailText}" Padding="3">Registation Date:</TextBlock>
                <TextBlock Style="{StaticResource detailText}" Padding="3">Sale:</TextBlock>
                <TextBlock Style="{StaticResource detailText}" Padding="3">Type:</TextBlock>
            </StackPanel>

            <StackPanel Margin="10 0 0 0" Grid.Column="1" HorizontalAlignment="Center" >
                <TextBlock Style="{StaticResource detailTextValue}">0908200585</TextBlock>
                <TextBlock Style="{StaticResource detailTextValue}">Nguyễn Hoàng Duy</TextBlock>
                <TextBlock Style="{StaticResource detailTextValue}">01/01/2001</TextBlock>
                <TextBlock Style="{StaticResource detailTextValue}">Hẻm 133 Hòa Hưng, phường 12,quận 10</TextBlock>
                <TextBlock Style="{StaticResource detailTextValue}">20/11/2021</TextBlock>
                <TextBlock Style="{StaticResource detailTextValue}">3</TextBlock>
                <TextBlock Background="{StaticResource Light Red 62}"  Foreground="{StaticResource Burnt Sienna}"
                           HorizontalAlignment="Left" Padding="7">Vàng</TextBlock>
            </StackPanel>
        </Grid>

        <!--Edit UC-->
        <uc:EditCustomerUC x:Name="EditCustomerUC" Grid.RowSpan="2" Visibility="Hidden"></uc:EditCustomerUC>

    </Grid>
</UserControl>
